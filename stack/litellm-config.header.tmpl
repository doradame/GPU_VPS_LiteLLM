# LiteLLM model list — generated by scripts/05-stack-config.sh.
# Re-run that script to regenerate after editing config.env.
# Key management and usage tracking are enabled by the DATABASE_URL and
# LITELLM_MASTER_KEY environment variables set in docker-compose.yml.
model_list:
